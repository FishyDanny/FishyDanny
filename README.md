```console
danny@sydney ~ % whoami
danny li · agentic engineer · sydney, australia
unsw — actuarial studies + computer science

danny@sydney ~ % cat about.txt
I build LLM agent systems and the full-stack products
they run inside. Constraint solvers, typed boundaries,
and tests that actually run in CI.

danny@sydney ~ % ls -l ~/shipping
drwx------  minishift/         rostering that refuses illegal shifts
drwx------  campsite-studios/  AI ops agents, run from Discord
drwxr-xr-x  recipe-browser/    recipes that survive being shared
drwxr-xr-x  road-safety/       crash risk models + explainable API

danny@sydney ~ % git log --author=danny --since=1.year --oneline | wc -l
     859   # most of it behind drwx------

danny@sydney ~ % cat stack.toml
languages = ["typescript", "python", "sql"]
web       = ["next.js", "react", "express", "prisma", "postgres"]
agents    = ["claude code", "opencode", "ollama"]
solvers   = ["or-tools cp-sat", "tensorflow", "shap"]
testing   = ["playwright", "jest", "pytest"]

danny@sydney ~ % ./hire-me --status
open to agentic/AI + full-stack roles · sydney or remote
mail  dannyliworking@gmail.com
in    linkedin.com/in/dannyxyli

danny@sydney ~ % ▊
```

**Live** [Recipe Browser](https://recipe-browser-lime.vercel.app) ·
**Code** [Road Safety](https://github.com/FishyDanny/Road-Safety-Risk-Prediction) ·
[P2P DB Vault](https://github.com/FishyDanny/simple_file_sharing_server)
